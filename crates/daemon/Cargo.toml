[package]
name = "sdi-daemon"
version.workspace = true
edition.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
license.workspace = true
description = "SDI background daemon (HTTP + Unix socket, holds SQLite state)."

[[bin]]
name = "sdid"
path = "src/main.rs"

[lib]
name = "sdi_daemon"
path = "src/lib.rs"

[dependencies]
sdi-core = { workspace = true }
sdi-db = { workspace = true }

tokio = { workspace = true }
tokio-stream = { workspace = true }
futures = { workspace = true }
axum = { workspace = true }
tower = { workspace = true }
tower-http = { workspace = true }
# llm-bridge proxy (D37): streams the SSE response from the Node sidecar back to
# the SPA. The `stream` feature (declared on the workspace dep) provides
# `Response::bytes_stream`; loopback HTTP only, so `default-features = false`
# (no TLS) on the workspace declaration is fine.
reqwest = { workspace = true }

serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
chrono = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
dirs = { workspace = true }
clap = { workspace = true }
ulid = { workspace = true }
slug = { workspace = true }

[dev-dependencies]
reqwest = { workspace = true }
